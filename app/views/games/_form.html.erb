<div class="form" id="new-game-form">New Game
<%= form_with(model: @game, local:true, class: "form") do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :description %>
<%= f.text_field :description %>
<%= f.submit %>
<% end %>
</div>
